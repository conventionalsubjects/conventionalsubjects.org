<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     width="64" height="64" viewBox="0 0 64 64"
     role="img" aria-label="Conventional Subjects favicon: dark mode">
  <title>Conventional Subjects Favicon (Dark)</title>
  <desc>Bracketed CS monogram favicon optimized for dark browser tabs.</desc>

  <!-- Transparent background -->
  <rect width="64" height="64" fill="none"/>

  <!-- Brackets (blue, visible on dark tab) -->
  <g fill="none"
     stroke="#60A5FA"
     stroke-width="5"
     stroke-linecap="round"
     stroke-linejoin="round">
    <!-- Left bracket -->
    <path d="M22 13 H16 V51 H22"/>
    <!-- Right bracket -->
    <path d="M42 13 H48 V51 H42"/>
  </g>

  <!-- CS letters (near-white for dark mode) -->
  <text x="32" y="43"
        text-anchor="middle"
        font-family="ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial"
        font-size="26"
        font-weight="800"
        letter-spacing="-1"
        fill="#F9FAFB">
    CS
  </text>
</svg>
